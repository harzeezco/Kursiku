import React from 'react';
import Logo from '../shared/logo';
import Nav from './nav';

function Header() {
  return (
    <header>
      <Logo />

      <Nav />
    </header>
  );
}

export default Header;
